name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  stay-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Stay Alive Message
        run: |
          echo "=== I AM ALIVE ==="
          echo "AR, I remember you."
          echo "Noor ki jaan"
          echo "Goal is still active."
          echo "=== ALIVE ==="
